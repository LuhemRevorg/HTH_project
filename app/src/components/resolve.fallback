const path = require('path');

module.exports = {
  // ... your existing configuration ...
  resolve: {
    fallback: {
      stream: require.resolve('stream-browserify'), // Polyfill for stream
      // Add other necessary polyfills if needed
    },
  },
  // ... rest of your configuration ...
};
